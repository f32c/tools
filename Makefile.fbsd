
ujprog:
	${CC} ${CFLAGS} -lftdi ujprog.c -o ujprog

install:
	install -m 4755 ujprog /usr/local/bin

clean:
	rm -f ujprog ujprog.o

depend:
	mkdep ${INCLUDES} ${SRCS}

SRCS = ujprog.c
INCLUDES = -I/usr/local/include
CFLAGS += -Wall -L/usr/local/lib ${INCLUDES}

